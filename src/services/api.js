import _ from 'lodash';

/**
 * Authenticates username and password in DB
 * @param {object} query the login request query
 * @returns {object} Response from server, shows if user is authenticated
 */
async function loginRequest(query) {

}